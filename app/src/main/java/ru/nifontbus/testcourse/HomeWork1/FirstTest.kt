package ru.nifontbus.testcourse.HomeWork1

import kotlin.math.pow

class FirstTest {
    companion object {
        val array1 = arrayOf(1, 3, 4, 8, -1)

        fun power(x: Double, y: Double) = x.pow(y)
    }
}